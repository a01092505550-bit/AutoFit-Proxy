services:
  - type: web
    name: autofit-proxy
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn proxy_server:app"
    envVars:
      - key: PORT
        value: 8080
